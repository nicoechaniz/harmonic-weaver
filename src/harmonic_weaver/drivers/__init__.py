"""Source drivers for harmonic-weaver. Each driver is a self-contained module
that receives an external stream and emits Source Frame v1 channel values
through the on_frame seam (see CORE_DESIGN.md §2)."""
